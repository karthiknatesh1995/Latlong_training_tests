
<%= form_for :formc, url: formc_index_path do |f| %>
	<% if @demo1.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@demo1.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @demo1.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
	<p>
	<%= f.label :title %>
	<%= f.text_field :title %>
	</p>
	<p>
	<%= f.label :comment %>
	<%= f.text_area :text %>
	</p>
	<p>
	<%= f.submit ("Click")%>
	</p>
	<%end%>
	<%= link_to 'Go Back', formc_index_path%>
	<%= link_to 'Home', welcome_index_path%>